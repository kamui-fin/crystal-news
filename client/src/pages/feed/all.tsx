import AppLayout from "components/AppLayout";
import Feed from "components/Feed";

const All = () => {
    return (
        <AppLayout>
            <Feed selectionType="ALL"/>
        </AppLayout>
    );
};

export default All;
